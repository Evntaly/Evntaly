# Evntaly Portal

Angular frontend for the Evntaly event analytics platform.

See the [root README](../README.md) for full documentation and Docker setup.

## Development

```bash
npm install
npm start
```

The portal runs at `http://localhost:8945` and expects the backend at `http://localhost:3000`.
